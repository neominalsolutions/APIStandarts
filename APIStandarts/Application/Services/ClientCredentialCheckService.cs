namespace APIStandarts.Application.Services
{
  public class ClientCredentialCheckService
  {

    public bool CheckClientCredentials(string clientId, string clientSecret)
    {
      // veri tabanı üzerinden böyle bir clientId ve clientSecret değeri olup olmadığını kontrol ettiğimiz yer.
      return true;
    }
  }
}
